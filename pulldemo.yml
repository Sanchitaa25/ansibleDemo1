---
- name: Hello World Playbook
  hosts: all
  become: yes

  tasks:

    - name: Print Hello World
      debug:
        msg: "Hello World from Ubuntu"

    - name: Update apt packages
      apt:
        update_cache: yes
